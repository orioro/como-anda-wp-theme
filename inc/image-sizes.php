<?php

/**
 * Used in link-image lists (width is most important)
 */
add_image_size('ca-link-image', 400, 800, false);

/**
 * Used in timelines (width is most important)
 */
add_image_size('ca-timeline-image', 600, 800, false);

/**
 * Used in the page template blocks
 */
add_image_size('ca-square-image', 500, 500, false);
